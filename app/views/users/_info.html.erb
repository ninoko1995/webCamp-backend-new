<%   now != 'books' ? @boo = 'default': @boo =  'primary'%>
<%   now != 'favorites' ? @fav = 'default': @fav =  'primary'%>
<%   now != 'followings' ? @wing = 'default': @wing =  'primary'%>
<%   now != 'followers' ? @wer = 'default': @wer =  'primary'%>

    <table class="table">
      <tr><td><%= image_tag profile_url(user) %></td></tr> 
      <tr><td>name : <%= user.name %></td></tr>
      <tr><td>introduction</td></tr>
    	<tr><td> <%= user.introduction %></td></tr>
    </table>

    <div class="row">
      <%= link_to "#{user.books.count} books",user_path(user),class: "btn btn-#{@boo} col-lg-6" %>

      <%= link_to "#{user.favorites.count} favorites",user_favorites_path(user),class: "btn  btn-#{@fav} col-lg-6" %>

      <% if correct_user?(user) %>
        <%= link_to "",edit_user_path(user),class: "btn btn-default col-lg-4 glyphicon glyphicon-wrench" %>
      <% else %>
        <% if user.followed_by?(current_user) %>
          <%= link_to "following",user_relationships_path(user),method: :delete,class: "btn btn-primary col-lg-4",remote: true %>
        <% else %>
          <%= link_to "follow",user_relationships_path(user),method: :post,class: "btn btn-default col-lg-4",remote: true %>
        <% end %>
      <% end  %>

      
      <%= link_to "#{user.followings.count} followings",follows_user_path(user),class: "btn btn-#{@wing} col-lg-4" %>
      <%= link_to "#{user.followers.count} followers",followers_user_path(user),class: "btn btn-#{@wer} col-lg-4" %>
</div>