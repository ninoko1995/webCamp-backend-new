<div class='row'>
  
  <div class='col-xs-3'>
    <h2>User info</h2>
    <%= render 'info', user: @user, now: 'followings'%>

    <!-- <h2>New book</h2>
    <%= render 'books/form', book: @book %>
 -->
  </div>
  

  <div class='col-xs-9'>
    <h2>follows</h2>
    <% if @accepted %>
      <%= render 'index', users: @follows %>
    <% else %>
      <%= render 'layouts/protected' %>
    <% end %>
  </div>

</div>