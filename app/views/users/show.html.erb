<div class='row'>
  <div class='col-xs-3'>
    <h2>User info</h2>
    <%= render 'info', user: @user,now: 'books'%>

    <!-- <h2>New book</h2>
    <%= render 'books/form', book: @book %>
 -->
  </div>

  <div class='col-xs-9'>
    <h2>Books</h2>
    <% if @accepted %>
      <%= render 'books/index',books: @books %>
    <% else %>
      <%= render 'layouts/protected' %>
    <% end %>
  </div>
</div>