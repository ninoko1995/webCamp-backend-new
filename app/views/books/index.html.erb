<div class='row'>

	<div class='col-xs-3'>
    <% if !current_user.blank? %>
	    <h2>User info</h2>
	    <%= render 'users/info', user: current_user,now: ''%>
	   <% end %>

    <h2>New book</h2>
    <%= render 'form', book: @book || Book.new %>

  </div>

  <div class='col-xs-9'>
    <h2>Books</h2>

    <%= render 'index' ,books: @books%>
  </div>
</div>